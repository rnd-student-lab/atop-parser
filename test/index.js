import { expect } from 'chai';
// import { atopLogsToObject, atopLogsToObjectStream } from '../src';

describe('Atop parser test suite.', () => {
  it('should work', () => {
    // TBD
    expect(true).to.be.deep.equal(true);
  });
});
